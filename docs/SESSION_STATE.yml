# Nimbus POS / ChefCloud — Session State

date: 2026-01-10
repo: mmazune/nimbuspos
branch: main
head_sha: c2465ffaf229da050eaa84111b0ab78b0676cd7d
objective: "Phase I1-I2 — Role Navigation Trees + Page Action Catalog"

definition_of_done:
  - Role nav tree generator created (scripts/analysis/generate-role-nav-trees.mjs)
  - Page action catalog generator created (scripts/analysis/generate-page-action-catalog.mjs)
  - All 11 roles mapped with nav groups and landing routes
  - 8+ high-traffic pages annotated with pageMeta
  - Reports generated (reports/navigation/)
  - Docs generated (docs/navigation/)
  - npm scripts added (nav:generate, actions:generate)
  - AI_INDEX.json updated with navigation pointers
  - lint+build pass
  - Atomic commit with proof

gates_required:
  - pnpm verify:no-wip-imports
  - pnpm -C services/api lint
  - pnpm -C apps/web lint
  - pnpm -C services/api build
  - pnpm -C apps/web build

notes: |
  Phase I is navigation/action documentation.
  I1 = Role navigation trees for all 11 roles
  I2 = Page action catalog with pageMeta convention
  Generators produce deterministic outputs for stable diffs.
